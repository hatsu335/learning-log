# [CheckNetwork_v3.bat]

### ネットワークに関する正常なPCと正常でないPCの設定比較

CheckNetword_v3.bat
```bash
@echo off
:: 文字化けを防ぐためにUTF-8モードに切り替え
chcp 65001 >nul
setlocal

:: 管理者権限チェックと昇格
openfiles >nul 2>&1
if %errorlevel% neq 0 (
    echo [Check] 管理者権限で再起動しています...
    powershell -Command "Start-Process '%~f0' -Verb RunAs"
    exit /b
)

echo ========================================
echo   PC設定取得スクリプト (Network Check)
echo ========================================

:: デスクトップパスをPowerShell経由で取得
for /f "usebackq delims=" %%a in (`powershell -NoProfile -Command "[System.Environment]::GetFolderPath('Desktop')"`) do set "DESKTOP=%%a"
set "OUT_FILE=%DESKTOP%\PC_Config_Report.txt"

echo [1/4] 保存先を確認: %OUT_FILE%

:: レポート作成開始（最初だけ新規作成、後は追記）
echo --- PC Configuration Report --- > "%OUT_FILE%"

echo [2/4] セキュリティ設定を取得中...
powershell -NoProfile -Command "Get-MpComputerStatus" >> "%OUT_FILE%" 2>&1

echo [3/4] ネットワーク詳細を取得中...
powershell -NoProfile -Command "Get-NetIPConfiguration -All" >> "%OUT_FILE%" 2>&1

echo [4/4] グループポリシーを取得中 (数秒かかります)...
gpresult /r >> "%OUT_FILE%" 2>&1

echo [*] レジストリ設定を追記中...
powershell -NoProfile -Command "Get-ItemProperty 'HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters'" >> "%OUT_FILE%" 2>&1
powershell -NoProfile -Command "Get-ItemProperty 'HKCU:\Software\Microsoft\Windows\CurrentVersion\Internet Settings'" >> "%OUT_FILE%" 2>&1
echo [*] 追加のネットワーク診断情報を取得中...
echo --- Routing Table --- >> "%OUT_FILE%"
route print >> "%OUT_FILE%" 2>&1

echo --- Hosts File --- >> "%OUT_FILE%"
type C:\Windows\System32\drivers\etc\hosts >> "%OUT_FILE%" 2>&1

echo --- Interface Metrics --- >> "%OUT_FILE%"
powershell -NoProfile -Command "Get-NetIPInterface | Select-Object InterfaceAlias, AddressFamily, InterfaceMetric, ConnectionState | Sort-Object InterfaceMetric" >> "%OUT_FILE%" 2>&1

echo.
echo ----------------------------------------
echo 完了しました！
echo デスクトップの「PC_Config_Report.txt」を開いてください。
echo ----------------------------------------
pause
```

正常なPCと異常なPCのレポートを比較する際、どこに注目すれば原因が特定できるか、**「見るべき優先順位」**に従ってチェックポイントを詳細にまとめました。

左手に「正常なPC」、右手に「異常なPC」のレポートを並べて、以下の順に照らし合わせてみてください。

---

### 1. IPアドレス設定の確認（最優先）

通信の基礎となる「住所」が正しく割り振られているかを確認します。

* **IPv4 Address（IPアドレス）**
* **チェック:** 両方のPCで最初の3つの数字（例：`192.168.1.xxx`）が同じかどうか。
* **注意点:** 異常なPCが **`169.254.x.x`** になっている場合、ルーターからIPアドレスを取得できていません（DHCPの失敗）。


* **Default Gateway（デフォルトゲートウェイ）**
* **チェック:** 正常なPCと**完全に一致**しているか。
* **注意点:** ここが空欄、あるいは違う数字だと、外のネットワーク（インターネット）へ一歩も出られません。


* **DHCP Enabled**
* **チェック:** 「Yes」か「No」か。
* **注意点:** 異常なPCだけ「No」で、かつIPアドレスがデタラメな場合は、手動設定によるミスが原因です。



---

### 2. 名前解決（DNS）の確認

「https://www.google.com/search?q=google.com」などの名前を住所に変換できているかを確認します。

* **DNS Servers**
* **チェック:** 正常なPCと同じIPが指定されているか。
* **注意点:** 異常なPCだけ特定のサーバー（以前のプロバイダや古い社内サーバーなど）を指していないか確認してください。


* **Hosts File（ホストファイル）**
* **チェック:** レポートの最後の方にある `hosts` ファイルの内容。
* **注意点:** `127.0.0.1  localhost` 以外に怪しいIPアドレスとドメイン名のペアが書かれていないか。ここに記載があると、DNS設定を無視してそこに繋ごうとします。



---

### 3. プロキシ・レジストリ設定（隠れた障壁）

「ブラウザだけ繋がらない」「特定のソフトだけダメ」という場合に重要です。

* **ProxyEnable（レジストリ）**
* **チェック:** `Internet Settings` 内の `ProxyEnable` の値。
* **注意点:** 正常なPCが `0` なのに、異常なPCが **`1`** になっている場合、存在しないプロキシサーバーを経由しようとして通信が止まっている可能性が非常に高いです。


* **ProxyServer**
* **チェック:** プロキシが有効な場合、そのサーバー名が正しいか。



---

### 4. グループポリシー（GPO）とセキュリティ

会社のPCなどで、管理ルールによって制限されていないかを確認します。

* **Applied GPOs（適用されたポリシー）**
* **チェック:** 異常なPCにだけ適用されている「制限系」のポリシー名がないか。
* **注意点:** 「Windowsファイアウォールの設定を強制する」といったポリシーが、異常なPCにだけ当たっている場合があります。


* **AntivirusEnabled (Windows Defender)**
* **チェック:** 両方とも `True` か。
* **注意点:** 別のセキュリティソフトが入っていて Defender が `False` になっている場合、その「別のソフト」のファイアウォールが原因かもしれません。



---

### 5. 経路と優先順位（高度な確認）

複数のネットワーク（Wi-Fiと有線など）がある場合に重要です。

* **InterfaceMetric（インターフェースメトリック）**
* **チェック:** 使いたい回線（有線LANなど）の数値が、他の回線より**小さく**なっているか。
* **注意点:** 数値が小さいほど優先されます。異常なPCでWi-Fiの数値の方が小さいと、不安定なWi-Fiを優先的に使おうとしてしまいます。


* **Routing Table (0.0.0.0)**
* **チェック:** `Destination 0.0.0.0`（すべての宛先）に対する `Gateway` が、上記1で確認したゲートウェイと一致しているか。



---

### 比較のコツ：効率的な探し方

1. **「ファイル比較ツール」を使う:**
もし可能であれば、`WinMerge` のようなテキスト比較ツールに2つのレポートを読み込ませると、**「違う部分」がハイライトされる**ので一瞬で原因が見つかります。
2. **下から上へ:**
意外と「レジストリのプロキシ設定」や「hostsファイル」といった後半部分に原因が隠れていることが多いです。

まずは **「1. IPアドレス」** と **「3. プロキシ設定」** を重点的に比較してみてください。もし「ここだけ数値が違うけれど、これが何を意味するかわからない」という箇所があれば、Go Gemini!


# [WinMerge]

### 設定ファイル等の差分チェック

**WinMergeはオープンソース界で20年以上の歴史を持つ、非常に信頼性が高く安全なツール**です。世界中のプログラマーやインフラエンジニアが日常的に「設定ファイルの差分チェック」に使用しています。

ただし、「安全に使い始めるための注意点」がいくつかあります。

---

## 1. なぜ「安全」だと言えるのか？

* **オープンソースソフトウェア (OSS):** プログラムの設計図が公開されており、世界中のエンジニアにチェックされています。悪意のあるコードを紛れ込ませるのが困難な仕組みです。
* **歴史と実績:** 2000年代初頭から開発が続いており、日本でも「窓の杜」や「Vector」といった老舗のソフトウェア紹介サイトで長年推奨されています。
* **広告・スパイウェアなし:** 寄付によって運営されているプロジェクトが多く、一般的なフリーソフトのような余計な広告や同梱ソフト（抱き合わせ）もありません。

## 2. インストール時の注意点（重要）

ツール自体は安全ですが、**「どこからダウンロードするか」**が重要です。偽サイトや広告リンクを踏まないよう、以下の正規ルートを推奨します。

* **公式サイト:** [WinMerge.org](https://winmerge.org/)
* **日本語版公式サイト:** [WinMerge日本語版](https://winmergejp.bitbucket.io/)（日本での利用はこちらが主流で、翻訳も正確です）

## 3. ネットワーク調査における「セキュリティ上の配慮」

WinMergeそのものは安全ですが、扱う**「データの内容」**には注意が必要です。

* **個人情報の扱い:** 今回作成した `PC_Config_Report.txt` には、PC名やIPアドレスが含まれています。WinMergeを自分のPC内で使う分には全く問題ありませんが、**比較結果をスクリーンショットなどで不用意にインターネット上に公開しない**ようにしてください。
* **オフライン動作:** WinMergeは基本的にローカル（PC内）で動くツールです。比較したデータが勝手に外部サーバーに送信されることはありません。

---

## WinMergeでの比較イメージ

今回の2つのファイルをWinMergeにドラッグ＆ドロップすると、以下のように表示されます。

* **背景が白い行:** 両方のPCで全く同じ設定（正常）
* **背景に色がついた行:** 設定が違う箇所（**ここがトラブルの原因！**）

> [!TIP]
> **インストールしたくない場合:**
> インストールが制限されている環境であれば、**「ポータブル版（zip形式）」**をダウンロードすれば、解凍して実行するだけで使えます。使い終わったらフォルダごと消すだけなので、PCを汚しません。


# [VirusTotal（ウイルス・トータル）]


「VirusTotal（ウイルス・トータル）」は、Googleの傘下企業が運営している**「世界中のセキュリティソフトを一度に集結させた、オンライン検診所」**のようなサイトです。

怪しいファイルやURLをアップロードすると、世界中の主要なセキュリティベンダー（カスペルスキー、ノートン、マカフィーなど）70種類以上のエンジンを使って、一斉にスキャンしてくれます。

---

## 1. 「チェック結果」で何がわかるのか？

チェックを実行すると、以下のような結果が表示されます。

* **検出率（Detection Ratio）**: 「70社中 0社が検知」となれば、世界中の主要ソフトが「安全」と判断したことになります。逆に数社でも「Malicious（悪意あり）」と出れば注意が必要です。
* **各社の判定名**: ウイルス名や、トロイの木馬、アドウェアなど、どのような脅威として認識されたかが具体的に表示されます。
* **詳細情報（Details）**: そのファイルがいつ作成されたか、どのような挙動をするかといった専門的なデータも確認できます。

---

## 2. なぜ VirusTotal を使うのか？

自分のPCに入っているセキュリティソフトは通常1種類だけです。しかし、A社では見逃されるウイルスがB社では見つかるということがよくあります。

* **多角的なチェック**: 70種類以上の視点でチェックするため、1つのソフトで判断するより圧倒的に精度が高まります。
* **最新情報の共有**: 新種のウイルスが発見されると、すぐに世界中のベンダーへ情報が共有される仕組みになっています。

---

## 3. 利用時の注意点（重要）

非常に便利なツールですが、以下の点には気をつけてください。

* **「0件」でも100%安全とは限らない**: まだ世に出て数時間の「超新種」の場合、どの会社もデータベースを更新できておらず、すり抜ける可能性があります。
* **機密ファイルはアップロードしない**: アップロードしたファイルは、世界中のセキュリティ研究者が分析のために閲覧できる状態になります。**社外秘の資料や個人情報が含まれるファイル**は、絶対にアップロードしてはいけません。
* **誤検知（False Positive）**: 安全なプログラムであっても、特殊な動きをするものは「怪しい」と判定される（誤検知）ことが稀にあります。1〜2社だけが反応している場合は、誤検知の可能性も考慮します。

---

## 4. WinMergeの安全性を確認する場合

もしWinMergeをダウンロードして不安であれば、ダウンロードしたインストーラー（.exeファイル）を [VirusTotalの公式サイト](https://www.virustotal.com/) にドラッグ＆ドロップしてみてください。

おそらく「0/70」といった結果が出るはずです。これが確認できれば、**「世界中のセキュリティ会社がこのファイルは安全だとお墨付きを与えている」**という確かな証拠になります。

